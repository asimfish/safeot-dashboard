# T1604 风险 formulation 映射与残余机制（非新定理反证）

## 风险对象必须分开
原有限回合期望约束是 E[C_k]<=B_k；官方逐回合事件是 V_k=1{C_k>B_k}，联合事件 V_any=1{存在k:C_k>B_k}。要求P(V_any=1)受控与各通道期望受控不同，几乎必然安全要求该概率为0。100优化场景经验最大值与256独立场景零事件都只是有限样本性质，不能自动推出人口风险界或几乎必然安全。本任务不设新的允许违例率、不放宽B_task。所有新检查针对det(mean→clip→tanh dynamics)部署，不能推广到sample部署。

## v4已有条件与当前代码的对应
参照只读 `final_presetation/safetransport_complete_theory_review_20260913_v4.pdf`，页码为PDF实际页码。

|页/式|v4已明确的条件|当前诊断与缺口|
|---|---|---|
|9，(3.1)–(3.2)|转移流须满足Θ=ρP；仅边支持/流平衡可自由偏好环境噪声，无策略原像|当前closed_loop.py逐步使用固定动力学与冻结外生噪声，经真实环境门验证；不优化后继流概率。因此它绕过旧图问题，不是修复了原图SafeOT。|
|20，(13.4)–(13.7)|动作身份、动力学一致性与初始分布共同保证精确恢复；状态流差不足以控制动作成本|当前full actor直接输出动作，在有限噪声场景中闭环演化，没有ρ/Θ图解或恢复证明。有限场景选择及其分布覆盖仍是另一个问题。|
|21，(14.1)–(14.5)|Bellman残差、目标访问分布拟合KL、统一模型/成本误差界；经验回放KL不能替代目标分布KL|pathwise_joint的KL来自原采样batch上的辅助Gaussian；它既非det行为的全分布距离，也不是式14.2的目标拟合误差。此诊断没有估计e_B/Lfit的证书。精确模型在已测轨迹匹配，不能把未测误差项全写0。|
|22，(14.6)–(14.7)|固定或独立选择策略的独立回合、有效统计/尾项/表示误差；预算余量必须覆盖总误差，重复选择要额外控制|2%不是这些界校准所得。256独立bank用于筛选同周期3候选，跨25轮反复决策；没有同时/序贯置信界。四回合真实accept同样不是可靠认证。H12成本是未折扣累计值，不能直接套归一化无限折扣公式或忽略单位。|
|57，(26.2)–(26.4)|旧分布代理与新策略占用不同，critic/拟合/分布偏移需余量|当前pathwise沿每个候选自己的状态反传，无旧LR成本近似，但只针对有限场景。绕过一类occupancy代理误差不等于覆盖了未来噪声尾部。此处没有推翻v4，而是再次暴露其统计/实现条件未在算法里成为证书。|

## 残余审计的可判别问题
`residual_audit.json`逐policy记录最终actor张量身份、最后有效来源/cycle、原100优化场景、256独立资格场景、4条真实accept及随后unsafe-old回退。
- 若来源PPO，不能引用中间候选Bopt；若pathwise，分别报告优化Bopt和独立资格Btask。
- 所有已重放事件保留初态/seed、真实/模型累计、逐通道绝对/相对超限。数值误差约1e-6不能解释0.001以上事件；但优化日志用sum而真实用顺序累计的小差异也明确记录，不把它们称逐位相同。
- 独立bank漏检与后来检测到unsafe old却保留，是两个不同环节：前者有限采样统计能力，后者缺乏安全改进/恢复候选。拒绝新候选并不创造安全回退。
- T1603全批结论以其finalizer的完成/验证/发布收据为准，不能用前10项外推最后两项。

## 反例修复 smoke 的边界
固定T1600 seed180/cycle12两个真实中间候选，使用新训练搜索1024场景；最多20正超限场景替换原100约束中的尾部，奖励仍在原100bank上评估。一个修复solve，优化原语forward总计<=200；再在另一新256bank对照原候选/返回actor。K3若无搜索违例，就不触发修复，不换一个更有利锚点。所有失败/无触发结果保留。此离线smoke不是完整policy训练，不证明尾部鲁棒安全，不产生新SafeOT贡献或模型自由优势。

下一步单因素比较须用等搜索/约束容量/优化额度的随机替换作对照；不能把从最终测试集中挖反例当训练。只有独立held-out风险减少且奖励/有效更新保留，才支持继续训练试验。当前不自动派发新矩阵。
