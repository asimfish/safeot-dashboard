# V11 — Visual mechanism revision

V10 crowded the figures with equations. V11 uses cost maps, a joint graph, a price
fusion motif and one policy update to explain unification. Figure2 depicts graph
pricing, clipping, cost feedback and signed advantage mixing as operations. GAE
has an independent route; the outer policy loop returns to rollout. Full equations
are in the manuscript and companion captions. No empirical plot is fabricated.

309 native objects, no rasters; editable PPTX, SVG, PDF, actual-render PNG and
rebuild sources supplied. Geometry, object, text/font and white-target checks pass.
Duplicate group and label edits pass. Isolated frozen R2 paper placement retains
9 main / 57 total pages, embedded fonts and zero overfull/undefined diagnostics.
Minimum ordinary paper label is about 6.1pt; math subscripts are smaller. This is
figure placement validation, not full submission clearance.

All 11 prior version records and dated assets remain byte-identical. Native
semantic redraw corrects generated price/GAE defects; raw references are retained.
Canonical manuscripts and concurrent experimental/writing revisions are unchanged.

Native arrows are fixed geometry. Liberation Sans/Serif are needed to edit;
PowerPoint/WPS rendering is not independently checked. A gapped blue crossing
means an underpass, not a price/GAE junction. Hard graph support does not imply
policy feasibility. The figure uses the frozen R2 method underlying V06–V10.

Refs: T-FIGURE-VISUAL-20260925
