# T-1661 — native V14 reconstruction

Two distinct method figures are restored to the main-paper plan. Figure 1 keeps the three-column teaser; Figure 2 keeps graph/policy bands. Native PDF/SVG and editable TikZ enforce 5.5in widths, 2.5/2.3in heights, actual minimum 7.1731pt and 45/37 English words. Metadata is stripped; all fonts are embedded; no raster artwork is embedded in either vector.

The comparison page defaults to V14 versus V16 and preserves every old asset. Figure 3 retains all 31 original a41 estimates/intervals, verified against four matching producer input hashes, and joins the supplied task counts plus all-16-task curves. A separate one-page appendix and real-data Figure 5(a) are included. The unsupplied warm-control task count is explicitly unavailable, not inferred; the later 686-row update is not silently mixed into this frozen candidate. Tables and historical figure IDs are retained. No submission asset links back to this site.

Validation: geometry/fonts/word budget/native/anonymity checks; explicit inspection of color, grayscale and print-scale method figures; source arrays retained in curve renderer; desktop/mobile browser and asset/hash checks recorded in the task run. Author adoption remains pending.

Refs: T-1661
