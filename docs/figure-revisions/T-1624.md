# T-1624 — Rich unified figure revision

The author selected the old 53-page manuscript as the content-density reference.
Figure1 now centers a shared price construction between the two endpoint views;
Figure2 separates graph-space pricing and policy-space learning while retaining
the full loop and independent GAE path. Both use the current method.

The new dated page and asset directory preserve prior pages, downloads and
registry records. The comparison includes the original legacy figures and four
unadopted generated images, clearly identified as having scientific defects.

Validation: both editable figures passed geometry, native-object, rendered-text
and sampled-fill checks. 358 native leaf objects; no raster artwork. Isolated
paper preview: 9 main pages, 57 total pages, embedded PDF fonts, zero overfull or
undefined diagnostics. Formula parts are grouped editable text and lines, not
OMML; module connections do not reroute automatically. PowerPoint/WPS-specific
rendering is unverified. The canonical manuscript was not replaced.

Local and live website checks are recorded in the task's dated private QA folder.
Refs: T-1624
