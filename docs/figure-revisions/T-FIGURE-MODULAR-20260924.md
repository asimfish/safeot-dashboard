# V10 — Modular figure refinement

The author liked V06–V09 content but asked for stronger modules, less clutter and
a clearer unified algorithm. Figure1 now encloses joint constraints, graph
optimization, safety prices and one policy update in a dominant central module;
the two endpoints are subordinate interpretations. Figure2 groups graph-price
conditioning and episode feedback inside a shared pricing module between rollout
inputs and the policy update. GAE reaches the update independently.

512 native objects, zero raster objects; PPTX/SVG/PDF/PNG, combined PPTX/PDF and
rebuild sources delivered. Real LibreOffice rendering, geometry, text, object,
sampled target-color and duplicate group/label-edit checks passed. An isolated R2
placement preview retains 9 main / 57 total pages, embedded fonts and no overfull
or undefined-reference diagnostics. This is not full submission clearance.

Math is grouped native text/lines, not OMML; arrows do not reroute automatically.
Liberation Sans and Liberation Serif are required for editing. PowerPoint/WPS
rendering is not independently checked. Smallest auxiliary labels are ~5.4–5.5pt
at the checked paper width. All ten old registry records and dated assets remain
unchanged. Generated references are retained and identified as unadopted.

Scientific scope is the frozen R2 price-based method used by V06–V09, not the
concurrently evolving experimental redesign. Canonical paper assets are unchanged.

Refs: T-FIGURE-MODULAR-20260924
