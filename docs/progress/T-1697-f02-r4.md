# R4: text-only terminology alignment

R4 is an exact R3 copy with four approved native text-node replacements. Every other ZIP entry is byte-identical; reverting those strings reproduces canonical slide XML. Actual300dpi pixel changes occur only within the four original label boxes. No fonts, layout, styling, equations, shapes or routes changed. The labels fit; text>=8.107pt and math>=8.169pt; zero raster objects. The same export pipeline produced PPTX/PDF/SVG/color/gray/CVD with HANDOFF. Cold review pending.183gallery records preserve181prior records and all images. The attached-PDF/current architecture branch remains separately available with its default comparison retained.

Refs: T-1697
