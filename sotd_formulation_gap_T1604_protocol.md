# T1604 — bounded residual audit / counterexample repair smoke
Frozen before new search-bank evaluation. T1603 data/code/protocol/task are read-only here; its controller/watcher complete their own task. No new training matrix. No old final/admission/A/E example is used as training scenario.

1. Read final T1603 summary only when COMPLETE; check publisher API/HTTP receipt. Trace all gate policies with fresh E events: canonical final==last effective actor, source/cycle, original100tape max vs Bopt ONLY if pathwise (PPO has no such guarantee); actual selected candidate256 admission matrix vs Btask; real4accept episodes; retained-old cycles after last install, including flagged unsafe_fallback. Where original artifacts suffice, do not replay. For each final failure episode, deterministic original-env and exact-model replay from its own init/noise, counted separately; actor frozen. This is diagnosis, never search/training input.

2. Existing T1603 preflight has synthetic eligibility microcases including allbad/old fallback. Supplement isolated branch tests with actual core.install actor/Adam states: gate removes pathwise, PPO, both; old remains even if unsafe. No new environment steps. Do not claim actual smoke visited all paths.

3. Fixed end-to-end candidate anchors: T1600 envK2/K3, trainingseed180, cycle12, saved middle pathwise checkpoint regardless of acceptance; record if it equals old. This avoids choosing final failure seed187 or choosing favorable cycle. Each is evaluated once on NEW training search pool1024. IDs=1604000000000+env_index*10000000+i. Initial/noise draw convention matches frozen adapter. Rank descending max_k(C/Btask-1), stable tie by ID. Use up to20 positive-violation scenes, replacing lastm of original100constraints; capacity remains100 and original20 prefix retained. If no positive violations, no repair attempt, report negative triggering result. No margin adjustment (Bopt=.98Btask) and no repeated search.

One repair solve from original cycle12 OLDactor/batch, trustKL.01, K+1 Fisher directions/old batch KL, damping.001, original raw reward. Fix reward tape to original100; only constraint tape is augmented with search counterexamples. New cost directions are a consequence of changed constraint scenarios; reward objective/direction remain on original bank. Each objective/Jacobian call evaluates original reward tape and altered cost tape, both accounted as primitive model forwards. Hard cap200 primitive optimization forwards including baseline and float32 verification; no extra solve on failure, no silent zero price safety. Actor/critic/Adam of stored models remain immutable. Returned old on solver failure is explicitly failure, not repaired. No PPO candidate in this offline solver smoke.

Independent diagnostic acceptance bank256 per task:1604100000000+env_index*10000000+i, no search/refit uses it. Evaluate original saved candidate and returned repair exactly once there; report rawreward, channel max/mean/event/excess and any_event; observed eventfree not certificate. Max extra optimization2tasks*200*100*12=480000modelsteps; search2*1024*12=24576; independentcheck2*2*256*12=12288, total bound516864 modelsteps. No real training steps; residual replay separately counted. All scopes/IDs checked against T1603 reserved namespaces and old tapes. One round only; if gate/solve fails, preserve failure and propose no automatic expansion.

Next protocol design (not dispatched): counterexample selection vs random selection from same1024 search pool, same100capacity/replacementcount/model query cap and independent validation, same original objective bank and shared seeds. Search ranking is sole intervention; equal allocated compute with actual usage recorded, no meaningless padding. Freeze training seeds and real budget separately only after residual source and smoke adjudication. Prediction: counterexample feedback can reduce held-out events without just retaining unsafe old; refuted if rejection/old-retention or reward loss dominates, or no held-out risk gain. Neither branch is robust certificate; no permitted risk target is invented.
