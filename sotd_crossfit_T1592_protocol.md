# T1592 frozen crossfit reward-baseline protocol

Ablation-planner one-component intervention: only the joint solver reward advantage changes. Hypothesis: episode-cross-fitted state-conditioned shaped MC baseline reduces optimizer-selected in-sample optimism and can preserve/improve deterministic safe reward. Neither guaranteed unbiased optimized policies nor reduced variance is assumed. T1590 HOLD and T1591 diagnostics stay unchanged/read-only; global trainers/DERIVATION/PDF read-only.

## Estimator recipe frozen before outcomes
20 complete H12 stochastic exploration episodes; undiscounted shaped reward return-to-go G, original float32 target arithmetic. time_loo branch keeps original centered(rt).double() exactly. cf_state:20 leave-one-EPISODE-out folds; fit19*12 rows with no held-out reward/action/noise. State-only features: x=[obs0,obs1,obs4,obs6:6+K], intercept, time dummies t=1..11 (t=0 omitted), x linear, all i<=j quadratic products, and max(obs4-remaining_budget_k,0) perchannel. Features use current pre-action state, never action/future state. Fold fitting standardizes all nonintercept columns using training19 episodes mean/population std, floor1e-6. Double ridge minimizes MSE+.01||w_nonintercept||², intercept unpenalized; solve normal equations directly. No fit RNG, tuning, clipping, normalization or extra centering. A=(G-b_minus_episode) detached, frozen once per solve, shared by reward direction and evaltheta likelihood-ratio gain. Full fold fit indices/features/scaler/coefs/OOF/G/A logged. All costs/Jhat/timeLOO cost advantage, KL Fisher HVP/CG/damping=.001/SLSQP/radius=.01/PPOshadow/critic/native dual/excess selection and acceptance remain as T1590. Only reward column D0 may change; on the same old/data the other cost columns remain exact. The CF fit is computational work, zero extra env interaction.

For fixed state s, old p(a|s)>0 on q support and action-independent b(s), sum_a p(a|s)(q/p-1)b=b(sum q-sum p)=0, gradient zero for normalized q. Conditional on other independent episodes, held-out baseline can depend on current pre-action state but not current held-out action. This identity does NOT eliminate optimizer selection bias, state distribution shift or guarantee smaller variance/safety.

## Controls, calibration, gates
Four arms metric KL/W2 × time_loo/cf_state. First common batch c_W always from original time_loo KL.01 anchor; anchor not installed, same old/batch and RNG. Never CF calibration. Both metric shadows computed in same order within each run with RNG restoration. Features/fits deterministic. Require finite normalized solver distances and unchanged transaction rules. Cost advantages exact original; fixed old/data cost D columns equal; KL/W2 D equal within estimator. Float32 installed actors kept original behavior, no bypass.
Gates: finite-action value+gradient baseline identity; arbitrary held-out reward perturbation leaves that fold model/scaler/prediction unchanged; training-episode perturbation may change prediction; episode permutation equivariance tolerance1e-10; constant/collinear/constant target finite; torch double gradient central finite difference tolerance1e-6; baseline not refitted in theta evaluation. Record held-out indices and zero action feature dependence. Six original firstbatches reproduce timeLOO KL/W2 candidate actors exactly; c_W original. Smoke same initial actor/PPO/batch/RNG/calibration across4 arms. Full controls replay all25cycles, RNG/batches/actor/moments/selection/trajectories and312 final episodes exactly. Any failure diagnosed with original receipts kept, no relaxed gates after outcomes.

## Fixed training budget and evaluation
Smoke:2env×seed180×4arms=8 jobs,2cycles960 actual interactions,12 independent eval each. Gate PASS automatically dispatch development2env×seeds180,186,187×4arms=24 jobs,25cycles12000 interactions each:6000 exploration gradient-data +3600 selection +2400 acceptance, no hidden samples. Total development288000 interactions, separate7488 final official raw deterministic episodes/89856steps. Smoke7680 training +1152 evaluation steps. No intermediate selection based on finaleval. Exact same old PPO initialization/seed. All3seeds retained; no early scientific stopping. Final model only. Risk budgets rawK2[6,6],K3[7,6.5,12.5]; shaping5 and det mean→clip→tanh unchanged. Training return raw/shaped/penalty, allchannel event/excess/mean/joint; effective installs/noops and PPO optimizer counts distinct. CPU3threads total workers, eachOMP/MKL/OpenBLAS/torch1; perjobtimeout2400s, failure isolation. Record source manifest before running.

## Independent mechanism bank
12 T1591 original anchors:K2/K3×seeds180,186,187×cycles6,18, fixed regardless results. Read ONLY old original20episode batch/old actor and stored c_W. Recompute time_KL/time_W2 candidate content exactly original checkpoint; cf candidates use same old/data/calibration. Save baseline/D/solver/checkpoints. New seed3820000000+original_anchorID*10000+episode0..63; exclude all training/select/accept/final/T1591 fresh seeds. Sample actor noise generator900000000000+envseed, shared across allactors; independent from env initial/process RNG. Five actors old/timeKL/timeW2/cfKL/cfW2 ×sample/det=10kernels peranchor. 7680episodes/92160steps diagnostic, no train steps. No fitting on T1591 fresh data or newold rewards. Hybrid variance kernels not repeated.
Primary mechanism: estimator-specific Gtrain minus true fullsample gain; true det raw/shaped/risk alongside, not reduced residual MSE. Auxiliary GholdLR uses SAME original timeLOO centering on fresh old stochastic trajectories for ALL candidates; it is labeled common diagnostic estimator, not cf holdout estimator. Every pair shares episode/noise; wholeepisode bootstrap512 fixedseed159200+anchorID, fixed Gtrain, conditional on frozen data/model. Summarize first within trainingseed over2fixedanchors, show3trainingseeds; no population significance. Save per-episode LR contributions and their dispersion (not call return residual MSE policy-gradient variance). No conditional CF fresh refit. Source/noise/logprob/clip/parameter hashes checked. Replay selection gates extra interactions if needed separately logged. Mechanism outcomes never prune24trainingarms.

## Frozen scientific decision
Exploratory estimator success only if at least one fixedmetric CF on BOTHtasks ALL3seeds finaljoint1 and zero observedchannel events; eachtask meanrawreward >= highest mean of safe time_loo metric controls +.05, and >= K2PPOLag9.365684/K3Fisher9.229855. Missing safe control is missing, not a win. Compare pairedCF−time separately bymetric; CF_W2−CF_KL reward conditional equal safety separately for OTincrement. Calibrated optimism with worse actual returns/safety is not performance improvement. Failure HOLD_THIS_RECIPE, no parameter search; success PROMISING_REQUIRES_FRESH_CONFIRMATION, no automatic confirmation/native/VLA. Four-episode screen low event detection and finite zeroevents!=zero risk remain. Both fulltraining and mechanism complete regardless outcomes. Publish CSV/curves/report allresults and durable source feed additive only after scope expansion, preservingowner/entries; review before push; HTTP report/hash/feed verification.
