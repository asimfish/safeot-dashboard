# Figure2 R2a — connector-only refinement

This refines one route in R2: the observed-cost-feedback output now uses the left edge of the policy module. It enters the combined-price equation without crossing the next-rollout connector. Scientific content, labels, fonts and dimensions are unchanged.

Run `python build_pair.py --figure 2`, `python build_editable.py`, and `python make_accessibility.py`. Dependencies are in requirements.txt. All labels are at least7pt at5.5in. Native PDF embeds LiberationSerif/STIXGeneral; outlined SVG is portable, text SVG and PPT retain editable text. Install the fonts before editing with a slide renderer. PDF is the publication reference.
