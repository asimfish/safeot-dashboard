# V17.1 data and naming supplement

This is a separate candidate snapshot, not the scheduled full V18 conceptual round. V17 remains unchanged. F1 A/B and F2 retain their previous candidates.

- Method display names: TRPO-Lag, PPO-Lag, CPO, PCPO, FOCOPS, CUP, CPPO-PID and TRPO-PID. Historical raw identifiers are preserved only in input provenance and alias mapping.
- Figure3 uses W2 newly exported 36-row forest: 34 observed effects with supplied intervals, two pending Bullet PCPO entries. SafePO matched controls and PCPO use source-defined seeds 0–4; other comparator seed counts remain as supplied. These estimates are different from the historical V17 cohort. No effect or CI was recomputed by drawing. Counts are exact counts of supplied task ratios below one.
- All-task learning curves retain their historical cohort and every original numerical value. They are not described as having the new forest cohort.
- Figure4 is a realization component candidate, not a replacement for the owner's complete realization/deployment composition. All 50 producer medians/intervals are gray because the planned30-run cohort is not final.
- Figure5 uses480 A3,120 E034 and24 E037 primary cell ratios verbatim. Four pending-secondary medians are the current six-cell W2 observations, not predicted ten-cell outcomes. Ratios are V(rule)/V(solve), explicitly labelled.
- Pending data: one gray hollow-diamond key. Where observations exist, actual values are shown in gray with their denominator; when absent, a location-free gray mark states pending. No missing estimate is placed at zero or parity. Pending_evidence is an internal review ledger, not a new experimental result.
- Gray/CVD previews, native-size print proofs and editable vector/Python sources are included. Owner admission and independent visual review remain pending.
