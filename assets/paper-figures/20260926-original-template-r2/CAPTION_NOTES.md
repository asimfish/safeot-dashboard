Figure 1. One price rule connects penalty and constraint views. SafeOT projects rollout flow under all soft budgets; hard constraints exclude edges. Clipped, smoothed safety prices combine with observed-cost feedback to weight the actor. Side curves, icons and graph weights are schematic. The bottom band distinguishes exact settings from conditional limits; it is not a numerical axis. Nodes are state clusters and edges are transitions.

Figure 2. One rollout becomes a priced policy update. Flow inputs, soft budgets and hard masks define the entropic projection under discounted flow conservation. Only safety prices pass to the actor; the target flow is not executed. Observed episode costs update beta, while reward and cost advantages enter the normalized actor objective. Here L is mean episode length, epsilon controls regularization, alpha is the feedback step, c the clip level, and H the excluded-edge set. Action-time enforcement requires a separate executor.
