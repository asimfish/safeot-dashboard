# F02 R5 — optional bounded polish

R4 is the accepted fallback, per the21:12Z mentor message. All R4 files remain byte-identical. R5 is a separate optional candidate and has no new cold-read score. The author’s original-PDF→editable-PPT mainline remains separate; this candidate does not replace it or the manuscript.

## Changes, restricted to(a)–(d)

(a) Both figures add an8.1pt orange “high cost” label with a0.6pt tick touching the upper route. Figure2 marks the observed before-graph, so the movement toward the lower target route is visibly cost-related. The dashed crossed edge remains a separate hard constraint; no claim that it itself caused the reallocation.
(b) Figure1 moves the four observed/target legend objects6pt left. Main graph, budget formula and header stay fixed.
(c) Figure2 panels1–3 now have approximately2.59pt bottom ink margins: observed episode-cost label/symbol move2.8pt up, flow-input formula moves13.3pt down, hard-constraint label moves0.4pt up. Only the start of the dashed observed-cost connector moves to follow its output; the rest of the feedback and next-rollout paths stay unchanged.
(d) Figure2 now reads “s.t. flow conservation + constraints” at its existing8.1pt size and position.

No other native objects change. No equations, star glyphs, budgets, alpha, advantages, methods, data, sizes or palette are revised. All mathematical child contours and LaTeX source are identical to R4; two translated formula groups retain their original glyph geometry.

## Deliverables

Figure1/2_F02_R5.pptx, .svg, .pdf, _300dpi.png, _grayscale.png, _deuteranopia.png and _protanopia.png. Same native-PPTX→LibreOffice export pipeline. Exact LaTeX, formula/font/object audits, editability probes, requirements, process notes, scripts and F02_R5_sources.zip are supplied.

Both pages remain5.5×3.3in; minimum exported label8.107pt, mathematical scripts8.169pt. All new labels fit without shrinking. Actual color/grayscale PPTX renders inspected. Zero raster objects; anonymous PDF/SVG. Native group-move and label-edit probes pass. Object XML and300dpi pixel differences are confined to approved objects/regions. R4 hash preservation passes.

Formulas are editable grouped vector contours, not semantic OfficeMath; LaTeX source is included. LibreOffice is verified; native PowerPoint rendering remains unverified. Owner promotion and any R5 acceptance remain separate.
