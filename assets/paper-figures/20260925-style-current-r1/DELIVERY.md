# SafeOT: modular style / current content — editable review pair

Figure1 is the unified perspective, without experiments. Figure2 is the current method framework. Both use a 5.5 by 3.3 inch print canvas, with text at least 8.107 pt and all mathematical glyphs, including sub/superscripts, at least 8.169 pt. Preview, SVG and PDF come from the actual editable PPTX exported by LibreOffice. The two slide decks contain zero raster objects. Each large module is a native group; normal labels are editable text and formulas are grouped vector outlines, with the exact LaTeX sources alongside. Formula groups are not semantic OfficeMath objects. PowerPoint rendering remains unverified.

The author requested the earlier visual language with current SafeOT semantics. The new F02 layouts are provisional development choices for review; no author/mentor acceptance is claimed. All generated sources and earlier editable revisions remain separate. These files have not replaced manuscript figures.

The gray and teal graph arrows are schematic observed and target flows; nodes denote clustered states. Constraint-view axes are the two graph cost coordinates, with bounds b1,b2. Only graph prices reach the policy update: F* is a target and is never executed. The exact-settings/conditional-limits band is categorical, not a quantitative axis. LP and saturated-clip connections require the assumptions in the paper. Experimental data are absent.

Color roles: navy conceptual headers, teal solve and graph prices, orange realized-cost feedback, gray standard RL, red dashed excluded edges. Grayscale and two color-vision simulations are supplied. Anonymous PDF/SVG exports contain no process or dashboard links.
