Create ONE refined publication figure using the attached S2 image ONLY as a layout reference. It is not scientific authority: repair its listed defects. Opaque WHITE canvas. Use FLAT solid pale fills #F4F8F8 or #F7F7F7, NOT gradients or shadows. Teal #167C80 means solved graph prices, orange #D55E00 appears only on beta feedback, gray #59636A means standard RL; purple is only the schematic baseline price. Hard-excluded edges are red dashed X. Crisp restrained rounded modules; no glossy panels. Landscape roughly2.1:1. Only the exact words/math requested below may appear; do not add explanation sentences, legends, duplicated titles, node letters, icon captions, mini-boxes or prose. Font hierarchy at a2200px-wide canvas: body/math42px, titles50px; math scripts must remain40px or larger. At final5.5in width every symbol must be>=7pt, including subscripts. Prefer fewer labels over smaller lettering. No personal names, URLs, watermark, candidate ID or conference name. Use Times-compatible typography and correct hats/bars/superscripts.
Scientific graph motif, when used: same SIX nodes in both states: left source; upper-left and upper-right nodes; lower-left and lower-right nodes; right sink. Edges source->upper-left->upper-right->sink form top route; source->lower-left->lower-right->sink form bottom route. One hard diagonal upper-left->lower-right is red dashed withX, excluded from both flow solutions; no positive mass is shown on it. Observed gray flow uses a THICK top route and THIN bottom route; target teal flow uses THIN top route and THICK bottom route. No extra edges, new nodes or reversed directions. Node labels are omitted to save text. This is schematic mass redistribution, not numerical experimental evidence.

FIGURE2. Exactly five numbered modular stages named "1 Rollout", "2 Flow inputs", "3 Budgets / exclusions", "4 SafeOT projection", "5 Priced update". Use gray for stages1,2,3,5; only stage4 gets a soft teal fill and is the largest. No image-wide title, no header banner, no extra descriptive sentences.
Stage1: pi_t plus one tiny trajectory pictogram. Exact compact outputs "GAE" with Ahat_r,Ahat_{c_k}, and Jbar_k. Do not expand words into sentences.
Stage2: one empirical graph, with Fhat,A,C_k as its port label. Node labels omitted.
Stage3: just <C_k,F> <= b_k and F_H=0, plus one small red dashed X edge. Two annotations only: "soft budgets", "hard exclusion". No episode-budget box, no matrix, no layer stacks.
Stage4: visible matched before/after rerouting Fhat -> Fstar using the specified SIX-node topology. The operation is min_{F>=0} -<A,F> + epsilon KL(F||Fhat), placed above the pair on two short lines. Constraint labels are exactly "flow balance", <C_k,F> <= b_k, F_H=0. Place "target flow is not executed" below Fstar. Raw dual lambda*_k leaves the projection through a distinct right-side port labeled "budget duals". This port is NOT an execution arrow from the Fstar graph.
Stage5: lambda*_k arrow MUST terminate at "clip + EMA" inside the actor module; its lambda_bar*_k output MUST visibly connect to the upper input of the price sum. The orange beta_k input enters the other input. Equation lambda_k = lambda_bar*_k + beta_k. Then the FULL normalized advantage Atilde=(Ahat_r - sum_k lambda_k Ahat_{c_k})/(1+sum_k lambda_k) is computed BEFORE "PPO / TRPO", whose output is pi_{t+1}. Keep all math correctly stacked, readable and >=7pt in print. No variables in freestanding peer boxes.
Required connectors: rollout graph data stage1->stage2; stage2 Fhat,A,C_k DIRECTLY tostage4, bypassing stage3 with a short upper route; stage3 b_k,H enters stage4 at a separate lower input. Direct GAE arrow stage1->the ADVANTAGE equation in stage5 via a lower lane, never through solve. Observed-cost arrow Jbar_k-B_k from stage1 feeds beta-update then the SUM in stage5 via a second lower lane; write beta_k <- [beta_k+eta(Jbar_k-B_k)]_+ as the compact line label. Feedback line is gray with only beta orange. The final return loop starts at pi_{t+1}, ends at pi_t, and has ONLY ONE arrowhead pointing INTO ROLLOUT. Do not add any arrowhead into the actor on that return loop. Avoid connector/label collisions and do not connect quantities to arbitrary box borders when the consumer is specified.

LAYOUT: preserve a SINGLE ROW of five large columns, with width weights12/17/13/33/25. Major boundaries align on top and bottom. Projection is dominant. Use reserved upper corridor from stage2 to4 over stage3 without entering it. Two short lower lanes carry GAE and observed costs into the named stage5 consumers. Avoid a heavy header band; compact bold titles only.