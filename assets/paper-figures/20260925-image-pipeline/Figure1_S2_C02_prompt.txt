Generate ONE complete research-paper figure candidate, not a slide, poster or contact sheet. Opaque pure white canvas, landscape approximately2.05:1 for Figure1 and2.25:1 for Figure2. Use a refined print-first academic drawing: precise flat vector-like lines, Times-compatible serif labels and properly typeset mathematical notation, soft pale module fills, restrained rounded corners, consistent0.6pt outlines and0.8pt arrow strokes at final print size. Request high resolution, ideally at least2400 pixels wide. At5.5in wide, EVERY label and mathematical subscript must remain >=7pt; use equivalent font size >=1.8% of image width, titles8.5-9pt equivalent. Enlarge needed labels, never hide detail in tiny captions. Module titles should be compact, not presentation-sized. No image-wide title, footer, watermark, panel ID, logo, personal identifier, URL or conference name.
Palette roles: teal #167C80 for graph solve and solved prices; orange #D55E00 used only as a small beta observed-cost-feedback accent; gray #59636A for standard RL, inputs and cost curves; muted purple #8064A2 for schematic baseline price curves if needed; red dashed X only for excluded hard edge. Soft tints are near-white. No gradients, shadows, glow, 3D, neon, glossy icon art or decorative matrices. Use simple meaningful graph/trajectory pictograms. Node identities match before and after. Edge thickness means schematic visitation, never invented numerical values. Labels and line patterns preserve semantics in grayscale.
HARD SOURCE CONTRACT: exactly one canonical SafeOT solve and one policy update; k indexes budgets on that same flow. Keep mathematical hats/bars/stars attached and subscripts stacked. Use lambda*_k for raw budget duals, lambda_bar*_k for clipped/EMA prices, beta_k for actual observed-cost feedback, lambda_k for their sum, b_k for per-step flow budget and B_k for episode budget. No capacity composition, min-capacity, certificates, Sinkhorn internals, flow importance weights, or claims of guaranteed policy feasibility. Parameters live on edges, ports or small tags, not freestanding peer modules. One bundled arrow between modules unless distinct inputs require separate paths. Every arrow has a true producer and consumer. No arrow routing GAE through the projection; no F* execution arrow. A before/after graph must visibly reroute thick flow away from a costly route. Hard events remove a dashed X edge, never assign it a finite price. Exclusion in a graph does not certify real actions. All extra assumptions go in the paper caption, not paragraph boxes. Preserve generous but purposeful whitespace and a dominant central mechanism. Do not add unrequested text or modules.

Figure number:1. Reviewer takeaway: One price rule solves graph-derived prices and combines them with realized feedback, spanning exact penalty settings and conditional LP/active-set limits.
LAYOUT GRAMMAR: Three columns18%,60%,18%,4% gutters. Left/right support plots smaller. Center has top horizontal observed graph -> entropic solve -> target graph, then a broad price equation with beta entering from a compact lower side leader, then actor. Bottom18% is a three-zone settings/SafeOT/limits band. Make price fusion the focal middle row.
FIGURE1 CONTENT. Match the earlier paper's modular THREE-COLUMN visual language but use ONLY the current method below.
Left support module: header "Fixed penalty", one mini-plot of cost rising above a dashed budget B while a dashed price stays fixed. A small shaded violation region; labels "cost", "price", "training" and "Budget leakage" only. This is schematic, no numerical ticks or data claims.
Right support module: header "Lagrangian", a mini-plot of cost oscillating about budget B and a dashed multiplier whose rise LAGS the cost violation. Label "Late feedback"; price is illustratively scaled, not a shared physical unit with cost. No claim that all Lagrangians always oscillate.
Center dominant module title "SafeOT", subtitle "One flow, all budgets". Show a rollout transition graph with6-8nodes, visible mid-gray observed flow Fhat and teal rerouted target Fstar. One red dashed excluded edge markedX disappears from the target. All soft budgets constrain this SAME flow: use compact mathematical tag <C_k,F> <= b_k, for all k. The central operation is "Entropic projection". Its raw budget-dual output arrow lambda*_k goes through "clip + EMA" to lambda_bar*_k. A short attached annotation says "Can price before violation". Then show lambda_k = lambda_bar*_k + beta_k, with orange beta input labeled "observed-cost feedback" and small Jbar_k-B_k on its arrow. Final arrow feeds "PPO / TRPO" actor through priced cost advantages; use Atilde as the carried output, no F* execution. The graph is a target; do not draw a robot following it.
Bottom compact semantic price-rule axis/band: five anchors "Fixed penalty", "Lagrangian", "SafeOT", "LP", "Active-set". Fixed penalty has c=0,beta fixed; Lagrangian has c=0,beta adaptive; SafeOT has finite epsilon; LP has epsilon->0; active-set has epsilon->infinity, clip saturated. Bracket the first two "Exact settings" and the final two "Conditional limits". It is a conceptual rule-axis, NOT a calibrated numerical continuum or a claim of recovering other algorithms. Emphasize the central SafeOT mark. No external algorithm marks. Avoid text-heavy descriptions: use math tags and pictograms, at most35 visible text groups.
