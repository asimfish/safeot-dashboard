Create ONE refined publication figure using the attached S2 image ONLY as a layout reference. It is not scientific authority: repair its listed defects. Opaque WHITE canvas. Use FLAT solid pale fills #F4F8F8 or #F7F7F7, NOT gradients or shadows. Teal #167C80 means solved graph prices, orange #D55E00 appears only on beta feedback, gray #59636A means standard RL; purple is only the schematic baseline price. Hard-excluded edges are red dashed X. Crisp restrained rounded modules; no glossy panels. Landscape roughly2.1:1. Only the exact words/math requested below may appear; do not add explanation sentences, legends, duplicated titles, node letters, icon captions, mini-boxes or prose. Font hierarchy at a2200px-wide canvas: body/math42px, titles50px; math scripts must remain40px or larger. At final5.5in width every symbol must be>=7pt, including subscripts. Prefer fewer labels over smaller lettering. No personal names, URLs, watermark, candidate ID or conference name. Use Times-compatible typography and correct hats/bars/superscripts.
Scientific graph motif, when used: same SIX nodes in both states: left source; upper-left and upper-right nodes; lower-left and lower-right nodes; right sink. Edges source->upper-left->upper-right->sink form top route; source->lower-left->lower-right->sink form bottom route. One hard diagonal upper-left->lower-right is red dashed withX, excluded from both flow solutions; no positive mass is shown on it. Observed gray flow uses a THICK top route and THIN bottom route; target teal flow uses THIN top route and THICK bottom route. No extra edges, new nodes or reversed directions. Node labels are omitted to save text. This is schematic mass redistribution, not numerical experimental evidence.

FIGURE1. Three large modules left22%,center52%,right22%,4%gutters, with a bottom summary band16%height. Titles are exactly "Fixed penalty", "SafeOT", "Lagrangian". The center is visually dominant; the side modules are smaller support schematics.
Left: one clean mini-plot. Solid gray cost gradually rises above dashed budget B, dashed purple price stays flat. Labels only "cost", "price", "B", "training", "Budget leakage". Right: solid gray cost oscillates about budget B, dashed purple price rises AFTER violations. Labels only "cost", "price", "B", "training", "Late feedback". No separate legends; no numeric ticks. These are schematic curves.
Center: top small subtitle "One flow, all budgets". Observed graph labeled Fhat, target graph labeled Fstar. Between them the actual operation label "Entropic projection". A small <C_k,F> <= b_k tag constrains the solve, not each edge individually. A red-X hard-edge tag uses only "hard exclusion". Draw an EXPLICIT downward arrow FROM the entropic projection operation labeled lambda*_k to the text "clip + EMA", followed by lambda_bar*_k. Near that raw-dual arrow attach "Can price before violation" without a new box.
Next row: clear equation lambda_k = lambda_bar*_k + beta_k. Orange Jbar_k-B_k enters a tiny orange beta-update tag from the LEFT, then beta_k enters the sum. Caption label only "observed-cost feedback". From the price-sum output, form priced advantage Atilde and THEN feed Atilde INTO a small gray "PPO / TRPO" actor box. The actor output is pi_{t+1}, NEVER Atilde. Keep this as one compact readable chain: lambda -> Atilde -> PPO/TRPO -> pi_{t+1}; lambda and Atilde are inline labels, not separate giant boxes. Fstar has no actor-execution arrow.
Bottom: one thin conceptual rule-axis with five anchors "Fixed penalty", "Lagrangian", "SafeOT", "LP", "Active-set". Compact math below: (c=0,beta fixed), (c=0,beta adaptive), (finite epsilon), (epsilon->0), (epsilon->infinity,clip saturated). Bracket first two as "Exact settings" and last two as "Conditional limits". No intermediate numerical ticks; no external algorithm names. Ensure each label remains visibly large. This unified rule-axis is the bottom summary, not another algorithm.

LAYOUT VARIANT: preserve the referenced three-column structure and a SINGLE overlaid graph. Replace the two graph states with the same six-node motif carrying thin mid-gray observed top-route strokes and offset teal target bottom-route strokes; a tiny two-line key uses Fhat(gray),Fstar(teal) ONLY. Do not put teal target flow on an isolated upper node: both top-route target edges are consistently thin. Attach Entropic projection to this in-place rerouting with a short leader; connect its dual output down to the price row. Make the lambda equation broad and central, with beta entering from left and advantage->actor at lower-right. The bottom rule-axis remains exactly as specified.