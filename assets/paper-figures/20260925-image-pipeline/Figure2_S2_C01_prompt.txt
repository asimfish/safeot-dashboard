Generate ONE complete research-paper figure candidate, not a slide, poster or contact sheet. Opaque pure white canvas, landscape approximately2.05:1 for Figure1 and2.25:1 for Figure2. Use a refined print-first academic drawing: precise flat vector-like lines, Times-compatible serif labels and properly typeset mathematical notation, soft pale module fills, restrained rounded corners, consistent0.6pt outlines and0.8pt arrow strokes at final print size. Request high resolution, ideally at least2400 pixels wide. At5.5in wide, EVERY label and mathematical subscript must remain >=7pt; use equivalent font size >=1.8% of image width, titles8.5-9pt equivalent. Enlarge needed labels, never hide detail in tiny captions. Module titles should be compact, not presentation-sized. No image-wide title, footer, watermark, panel ID, logo, personal identifier, URL or conference name.
Palette roles: teal #167C80 for graph solve and solved prices; orange #D55E00 used only as a small beta observed-cost-feedback accent; gray #59636A for standard RL, inputs and cost curves; muted purple #8064A2 for schematic baseline price curves if needed; red dashed X only for excluded hard edge. Soft tints are near-white. No gradients, shadows, glow, 3D, neon, glossy icon art or decorative matrices. Use simple meaningful graph/trajectory pictograms. Node identities match before and after. Edge thickness means schematic visitation, never invented numerical values. Labels and line patterns preserve semantics in grayscale.
HARD SOURCE CONTRACT: exactly one canonical SafeOT solve and one policy update; k indexes budgets on that same flow. Keep mathematical hats/bars/stars attached and subscripts stacked. Use lambda*_k for raw budget duals, lambda_bar*_k for clipped/EMA prices, beta_k for actual observed-cost feedback, lambda_k for their sum, b_k for per-step flow budget and B_k for episode budget. No capacity composition, min-capacity, certificates, Sinkhorn internals, flow importance weights, or claims of guaranteed policy feasibility. Parameters live on edges, ports or small tags, not freestanding peer modules. One bundled arrow between modules unless distinct inputs require separate paths. Every arrow has a true producer and consumer. No arrow routing GAE through the projection; no F* execution arrow. A before/after graph must visibly reroute thick flow away from a costly route. Hard events remove a dashed X edge, never assign it a finite price. Exclusion in a graph does not certify real actions. All extra assumptions go in the paper caption, not paragraph boxes. Preserve generous but purposeful whitespace and a dominant central mechanism. Do not add unrequested text or modules.

Figure number:2. Reviewer takeaway: One budgeted graph projection provides safety prices; observed costs correct them, and only prices enter the standard actor.
LAYOUT GRAMMAR: One row of five clearly separated numbered modules. Approximate module area weights12/18/17/30/23; the teal projection is largest. One left-to-right reading order. Keep the direct GAE-to-actor bypass and the observed-cost feedback in two short bottom corridors; neither enters the constraint-setting module. Bottom thin next-rollout arrow returns from actor to rollout.
FIGURE2 CONTENT. A method framework named "SafeOT" with exactly FIVE clearly separated numbered stages, gray standard RL and a larger teal projection as the focal point.
1 "Rollout": pi_t trajectory icon plus GAE outputs Ahat_r and Ahat_c_k; separately observed episode costs Jbar_k.
2 "Flow inputs": empirical transition graph. Nodes are state clusters and edges are transitions. Put Fhat,A,C_k on a bundled graph-input port; no three identical matrix icons.
3 "Budgets / exclusions": all soft b_k tags constrain the same graph, and one hard-edge red dashed X illustrates F_H=0. These are constraints, not learned prices or per-edge capacity allocation. Keep this module compact.
4 "Entropic projection": largest teal module. A before/after matched graph Fhat -> Fstar visibly reroutes visitation away from costly edges. Include compact objective min_{F>=0} -<A,F> + epsilon KL(F||Fhat), and short constraint rows: "flow balance", <C_k,F> <= b_k, F_H=0. The raw dual output is lambda*_k, labeled "budget duals". Attach "target flow is not executed" to Fstar with a short leader, not a forward actor arrow. Show the operation causing the rerouting rather than two unrelated graphs.
5 "Priced update": lambda*_k goes through clip+EMA to lambda_bar*_k, then lambda_k = lambda_bar*_k + beta_k. Show the actual normalized priced advantage Atilde=(Ahat_r - sum_k lambda_k Ahat_c_k)/(1+sum_k lambda_k), then a small standard gray "PPO / TRPO" step produces pi_{t+1}. Keep superscripts and subscripts correct, with all scripts>=7pt atprint.
Only solved PRICES flow from stage4 to5. Direct GAE advantages connect stage1 to the priced-advantage consumer in stage5, bypassing stages2-4. Observed costs travel on a separate lower feedback connector Jbar_k-B_k -> beta_k -> the price sum; beta <- [beta+eta(Jbar_k-B_k)]_+ may appear as one short line label. Feedback is standard gray with a small orange beta accent, not a giant orange module. A thin return arrow "next rollout" closes pi_{t+1} to stage1. Stage numbers convey execution order; do not invent a data relay through stage3. One canonical pipeline, no separate pipeline per cost or policy variant. No solver internals or execution certificate. Sparse labels, at most60 words excluding math.
