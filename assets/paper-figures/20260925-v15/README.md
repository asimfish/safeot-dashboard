# V15 teaser refinement candidate

V14 remains the author-preferred teaser baseline. V15 r03 follows V14 and reduces nested boxes, cleans up the joint graph motif, distinguishes exact controller settings from conditional graph-price limits, and makes the shared price enter the priced-advantage group before PPO/TRPO. The separate V14 detailed method framework remains a separate artifact and must not be replaced by the experiment figure.

These are generated raster candidates. PDF previews embed the raster at5.5in; they are not native vectors or editable PPT. The matching two-page V14/V15 proof uses the same5.5in image width. Exact raster font sizes cannot be certified. Candidate r03 still needs the author's aesthetic acceptance. The price arrow points to the operation label rather than the exact formula-box border; native reconstruction should use a precise port.

r01 andr02 are retained process drafts with known connector/scope issues; see the audit. All generation uses the built-in image tool; no specific provider model identifier is claimed.

Two graph-price endpoint labels are conditional, and the large-epsilon saturated endpoint is not asserted to equal the graph-free switch implementation. Hard-edge exclusion is a graph support operation and is not a deployed-policy safety guarantee. Detailed assumptions and the separate typed executor belong to the text/framework description.
