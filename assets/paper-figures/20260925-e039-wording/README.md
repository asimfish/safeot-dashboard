# Figure pair: wording refinement

One flow solve prices all budgets. These diagrams describe the mechanism and empirical training-cost results; robot gains are not attributed to budget coupling.

The prior modular layout, all formulas, data points, colors, connector routes and dimensions are preserved. Only three module labels and the two captions were revised. Standard RL remains gray.

Run `python build_pair.py`, `python build_editable.py`, and `python make_accessibility.py`. Dependencies are in requirements.txt. All labels are at least 7 pt at 5.5 in. The PDF embeds fonts; outlined SVG is portable, while the text SVG and PPT retain editable text. PDF is the publication reference.
