# Original-template R3: bounded print reviews

The user approved R2’s direction and requested less clutter, a better low-saturation palette, more attractive graphs and fewer formulas. The native R2 PPTs are the source; the old template's three-column/five-stage grammar remains.

Round1 / build01: replaced four colorful cost icons and two rollout raster icons with coherent native line pictograms. Replaced ten-node circuit grids with six-node, two-route schematics. Lowered blue/green/warm saturation and removed redundant equations. Actual exports exposed two paint-order defects: new circular badges covered the numbers, and the observed-cost route crossed its label. Figure1's cost arrow was also slightly off the graph inlet; Figure2's feedback needed a visible beta connection.

Round2 / build02: restored badge number paint order, placed the white label backing above the dashed route, aligned the graph inlet, joined observed cost error to beta, and moved the safety-price outlet to the solver heading. Removed unused raster relationships. Actual renders show the original modular hierarchy with a single teaser price equation and two framework core equations. Endpoint settings and compact variable labels remain.

Round3 / build03: grouped each graph and pictogram as an editable unit inside its native panel. No further visual redesign. Verify the exported image against round2, inspect grayscale/CVD, check text/formula sizes and test moving a graph/icon plus editing a native label. Keep all process previews; do not assign independent-reader scores.

Scientific coverage: this is a conceptual diagram, not measured data. The original schematic traces are preserved. The full flow projection, budget conversion and feedback recurrence are moved to the companion details for the caption/text. Hard masks concern graph edges; action-time exclusion requires a separate executor. All-budget pricing is a mechanism description, not a causal claim about performance.
