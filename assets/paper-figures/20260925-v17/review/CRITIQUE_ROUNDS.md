# V17 print-size critique record

Independent cold-reader acceptance remains pending. These are internal rendering critiques, not substituted 4/5 scores.

## Round 1 (07:52–07:55Z)
- F1 A/B: rerouting and binding face now visible. Left feedback equation crowded the narrow column; replace with adaptive beta. Label the outside point greedy. Make the center price stage explicit. Strip height 2.75 in and full Bullet median peak retained; area above median curve is not the registered V statistic.
- F2: the full constrained projection and grey standard RL create the required hierarchy. Add the rollout-to-graph statistics connector; keep target-only dashed relationship separate from the solid price path. At 2.569 in, height exceeds the older 2.3-in limit: latest full-constraint intent requires owner review of the additional 0.269 in; do not shrink fonts to hide it.
- F3: reversed V axis now gives the same rightward improvement direction as return. Row guides, repeated comparator labels and a separate ablation group aid tracking. Fix the unsupported diamond glyph in the count header by drawing a native marker. Retain all coordinates/CI values. Only two IQR bands remain visible; all source quartiles stay unchanged.
- Data issue: primary SafePO five-seed V=.374 differs from the frozen forest broader-cohort .336; primary Bullet ratio-of-means .221 differs from the forest task-median .301. Sent explicit handoffs to owner and W2. No estimates changed. Await producer-defined final-cost dagger counts; no invented threshold.

The dagger qualification uses the existing budget boundary C_final > B (task seed mean), not an invented threshold for “far above”. The producer table gives 4/6 TRPO-Lag and 5/6 PPO-Lag tasks; receipt retains all 12 supplied task means and hashes. Owner/W2 receive this precise wording.

## Round 2 (07:57–08:00Z)
- F1: the narrow left column now has exact-setting labels instead of a long update equation. The greedy exterior point is labelled; the full Bullet median peak is visible. Restore the word “ratio” to the V annotation (a primary V ratio must not look like an absolute V). Keep the distinct-estimand disclosure in the short caption and appendix receipt.
- F2: source graph input is connected. Main solid paths carry prices and advantages; the dashed realization relation does not feed F* to the actor. Try a compact 2.3-in version by removing vertical whitespace only, preserving all fonts and constraints; keep this 2.569-in round as a fallback.
- F3: markers now render natively without missing glyphs. The 4/6 and 5/6 dagger counts are explicitly task-mean final evaluation cost > B, directly counted from the producer's supplied means. Curves retain unsmoothed data; bands are intentionally limited to ours/Lag. No additional layout change planned before final print/CVD inspection.

## Round 3 (08:00–08:07Z)
- F1 A/B: inspected physical-width page proof and CVD/gray alternatives. The graph now shows the route change, the binding face and the shared price-to-actor rule. The primary-ratio/median-curve distinction is explicit in the 60-word B caption and source receipt. Compact A remains the user-selectable fallback.
- F2: compacted vertical whitespace to 2.3 in without reducing font sizes. Moved clip/EMA label away from the dashed realization mark; corrected connector endpoints after expanding the lower box margin. Full Eq. (3) and target-only semantics remain. No clipped page labels; the editable source retains all equations.
- F3: checked color, gray and deuteranopia previews. Solid/circle versus dashed/triangle/dotted/diamond encodings keep curves distinguishable when colors converge. At 5.5 in, dense forest rows retain 7.2-pt labels; direction headers and repeated comparator names reduce tracking effort. The 16-task appendix is retained.
- Limit: acceptance on message, hierarchy and top-venue quality remains for the mentor's independent cold-reader tests. Three internal rounds are complete; no claim of a 4/5 result is made. The cohort/estimator handoff remains a scientific integration issue, not a cosmetic fix.
