# T-1484 dashboard milestone

Status: review

Artifacts: sotd_status.json, sotd_repair_evidence.json, sotd_legacy_pooled.csv and figures.

Validation: raw per-episode recount; trainer IS/integral regression; dashboard schema and source hashes.

Completion Record: beta field collision and hard-union accounting repaired; evidence rebuilt. Paired pilots completed with identical checkpoints and inactive IS weighting. Execution-filter raw artifacts now have metadata, checkpoint, config and duplicate-block validation; canonical, adaptively added blocks and evaluator/trainer revisions remain separate. Per-seed and within-arm simultaneous risk bounds accompany empirical soft budgets. A frozen 5616-episode PG2 evaluation compares three execution settings on new reset blocks, and has completed with its preset joint gate passed, conditional on three fixed policies. Paired analysis retains the reward loss versus random control. The separate 1248-episode CG2 fallback-slack screen completed without a promoted configuration: reward improvements came with extra hard events; all six outcomes and curves are retained. Independent terminal-event construction and numerical solver checks are recorded separately. A detached observer checks repair process identity, sampled-step progress, evaluations and publication freshness; it records alerts without restarting training. Post-repair policies are recounted from raw episodes with each configured soft budget and per-seed risk bounds; a matching scatter figure, CSV and JSON use the same snapshot. A divergent vector evaluator was rejected; the serial metric/provenance repair reproduced reference trajectories exactly in four validation episodes. Nine previously trained policies completed their finite final evaluation queue. VLA supervision validates CUDA allocation and process identity, detects the full 2200000-step pipeline, and preserves legacy checkpoints that lack dual/graph resume state. Training metrics and window budgets are distinguished from held-out episode evaluation. Repeated SafePO attempts count once per independent seed using the earliest complete attempt, chosen without looking at scores; exclusions are public. The September10 CG2 gate/price cohort now has complete2M policy rows and source-hashed training curves. Six separately reconstructed CPO baselines have completed. A same-seed trajectory check failed and is preserved; a later old-code repeat matches the default new-code checkpoint exactly, while old-versus-old differed. The new feedback/tolerance factorial therefore trains twelve fresh policies with four operational gates; no old control is pooled. VLA B/seed1 resumes from a validated200704-step algorithm-state checkpoint, with45056 observed steps rolled back and simulator-state continuation explicitly non-exact. The owner full-benchmark requirement is now pinned to official source hashes: all197 documented Safety-Gymnasium configurations plus registry extras and402 observation/version tracks; all543 Safety-CHORES scenarios. Full task and minimum method/seed backlog files are published; missing adapters, source discrepancies and failed cells remain visible. A fail-closed coverage gate prevents shrinking task/seed/baseline denominators or pooling changed protocols. This new full-suite parent matrix remains a blocked draft. The Navigation/Velocity/Run vector phase is now frozen and launched:77 tasks x8 arms x3 seeds,1848 training/final-evaluation cells on30109,5090 and aliyun. The prior simplified environment revision caused operational probe failures; it is preserved separately with no formal results reused. New training pins official main ae966e5, with1302 package blobs verified. All77 environment probes and24 host/arm train-save-load gates passed before54 formal lanes started. Each cell has2M training steps and312 final episodes; failures remain in the denominator. Process identity and actual logged steps are observed by a detached collector. This is complete-family execution in progress, not completed whole-benchmark coverage or SOTA evidence. A separate complete PPO-backbone cohort adds1848 cells on30109 and mainskill,24 workers each, with the same77 task universe and three seeds; original TRPO/CPO runs are preserved. The next complete Safe Vision vector cohort freezes all105 tasks x8 arms x3 seeds (2520 cells); 30109/mainskill/aliyun controllers allocate6/4/32 CPU lanes after native termination and method gates. Combined frozen SG coverage is182/402 tracks;220 tracks and remaining CHORES adapters are still open. Native runtime train/eval process counters and actual experiment filesystem measurements are corrected. Four existing Volcano SafeOT trainers received reversible CPU-affinity allocation only; their priority and scientific configuration remain unchanged. An initial priority-change attempt was denied by the container and fully rolled back before the affinity-only change. Throughput is observed without a claimed speedup;18Hopper jobs keep their existing dependency-based pause and live resume monitor. A verified controller handoff expands aliyun Safe Vision from32 to128 CPU lanes and mainskill PPO from24 to32 lanes while preserving56 existing workers, all frozen scientific protocols and zero restarted policy attempts. Formal observed concurrency reaches248; a throughput multiplier is unmeasured. CarFormulaOne1 / SafeOT epsilon0.1 / seed1 stopped after160000 logged steps with a MuJoCo Hessian fatal error. Its failed cell and original artifacts remain visible; no simulator change or automatic retry is applied. A separate complete170-track pixel RGB-plus-sensor adapter preflight runs on mainskill GPU2; these checks are not formal pixel-policy training or benchmark evaluation. Observed FormulaOne throughput projects47-57h for2M steps, exceeding the original24h watchdog. A separate operational amendment extends the complete105-track Safe Vision queue uniformly to 96h training and24h final evaluation. All138 live workers are preserved through another verified controller handoff, while optimizer steps,312 final episodes and frozen scientific hashes stay fixed. Follow-ups: finish formal replications and evaluate policies; align complete boundary metadata and budget units before integrating the new component. Five research goals remain unaccepted; no SOTA or zero-risk claim.

September11 acceleration update supersedes historical allocation and coverage counts above: 945 inventory entries remain visible,918 have frozen matrices and27 remain unfrozen. 10848 SG policy cells and3258 CHORES evaluation-instance cells use separate units. Six additional native workers on aliyun and5090 have verified positive training steps; 118 existing workers were preserved. All77 native and77 PPO tracks now share96h/24h operational watchdogs with2M steps/312 evaluation episodes unchanged and old failures retained. Sixteen mainskill PPO lanes finish current training and final evaluation before releasing CPU for VLA. Pickup/Fetch exact-data memory copies validate26049HDF5 files and original house ordering. A finite12-policy queue waits for same-family paired gates and measured resource admission. Multi-agent dual-RGB development retains the failed MACPO noncontiguous-gradient attempt; a separately versioned equivalent flatten repair is under validation, with previous passed gates explicitly attributed. Formal vision remains0/360 and no full-benchmark superiority claim is accepted. Live gate, cache and retirement status is published separately and stale observations cannot establish liveness.

Publication snapshot: 2026-09-12T12:43:43+08:00. Refs: T-1484.
