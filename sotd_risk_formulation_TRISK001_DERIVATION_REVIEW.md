# SafeOT 风险 formulation 修订：估计什么策略、验证什么风险

任务：T-RISK-001。2026-09-16 起草，基于 T1593 完成结果。

## Target

回答两个具体问题：把平均成本换成首次超限概率，是否就能解决安全泄漏？把四回合检查换成置信上界，是否能在原预算下修复性能？

答案都是“不能直接推出”。本说明修正上一轮导师指导的目标、统计和预算矛盾；不修改既有 PDF、训练算法或结果，不声称已获得更好策略。T1594 实验 agent 暂因模型容量错误未继续执行，以下是导师侧独立的零交互分析。

## Status

**COHERENT AFTER REFRAMING / EXTRA ASSUMPTION**。

首次超限的代数表达和固定策略的置信区间可以严格成立；用随机策略的数据为确定性执行策略提供安全保证，以及在 12k 步预算内逐轮认证，仍有未解决的条件。当前不是完整安全算法的证明。

## Invariant Object

固定环境、初始分布、任务预算和实际执行方式。最终比较的是实际执行策略的原始奖励、任务成功率、逐通道超限概率、任意通道超限概率和超额量。

K2/K3 是累计成本诊断任务，不自动等于“玻璃破碎硬事件＋位置偏差软约束”的物理异构任务。将空间成本的起始阈值当作必须永不越过的硬边界，会改变任务，旧可行性审计已指出这种严格边界与原成功目标不相容。

## Assumptions

1. 现有诊断任务时域 H=12，逐步成本非负；按原环境 float32 顺序累加，并以严格大于预算作为事件。
2. 策略参数、执行核、初始分布和环境固定后，独立回合的事件指示服从同一 Bernoulli 分布。固定样本量 CP 区间需要此条件；不能把候选筛选、任意停止和策略变更藏进去。
3. 一条已训练策略及其冻结候选的独立评估，只提供该策略的评估信息；不提供训练种子总体的置信区间。
4. 训练动作来自非退化高斯分布；官方部署为网络均值动作经过裁剪，随后由环境执行。
5. 以下局部代理仍使用旧状态分布、有限样本和近似优势。其数值可行性不是总体概率保证。

## Notation

|符号|含义|
|---|---|
|C_k=Σ_t c_{t,k}, B_k|第 k 通道的回合累计成本与原预算|
|e_{t,k}, z_{t,k}|首次超限指示、此前是否已超限的状态标志|
|πθ, βθ|训练时随机策略、实际部署策略|
|R_k(β)|Prβ(C_k>B_k)|
|R_any(β)|Prβ(存在 k，使 C_k>B_k)|
|δrisk|容许事件率，仅由任务规格决定|
|α|置信失败概率，不能与 δrisk 混用|
|Uα(v,n)|n 回合观察到 v 个事件时的单侧 CP 上界|

## Derivation Strategy

先保持真实风险对象，再给首次超限的精确表达；然后分析执行核和局部代理之间的缺口；最后检验统计保证与预算能否同时满足。是否提高性能另由训练对照判断。

## Derivation Map

1. 非负累计成本 → 首次超限与回合事件的精确恒等式。
2. 高斯随机策略 → 确定性执行是策略变更，不是免费评估变换。
3. 同执行核的固定策略 → 二项分布上界及样本量。
4. 局部风险代理 → 需要估计、分布偏移、拟合、执行误差条件。
5. 已有轨迹复算 → 选择下一项判别实验，而不是把形式修正称为性能修复。

## Main Derivation

### 1. Identity：首次超限是正确风险语义，但不会自动创造数据

令 S_{t,k}=Σ_{j≤t}c_{j,k}，z_{t,k}=1{此前已经出现 S_{j,k}>B_k}。定义

\[
e_{t,k}=(1-z_{t,k})\,\mathbf1\{S_{t,k}>B_k\}.
\]

每条轨迹至多计一次。由于成本非负，一旦超过预算不再返回预算内，故

\[
\sum_t e_{t,k}=\mathbf1\{C_k>B_k\},\qquad
\mathbb E_\beta\sum_t e_{t,k}=R_k(\beta).
\]

任意通道事件另定义 union 标志，不能将逐通道概率之和当作精确 union 概率。若成本有负项，“途中超限”和“最终超限”不再等价，需要重新指定事件。若研究物理灾难，应使用真实事件检测器。

**数据复算。** 从 T1593 选 SS/RS 的 24 个作业，排除已证实完全相同的 outer 配对，不新增轨迹。在 600 个周期、1,500 个批次×通道中：首次超限优势全零 53 个，非零 1,447 个；原连续成本优势全零 0 个；累计成本及事件与原日志完全一致。不同周期/作业仍相关，不能把 1,500 当作独立训练重复。

这不支持“首次超限在本批随机训练数据中普遍没有信号”。非零优势也不证明风险梯度有效，更不证明它对应部署风险。

### 2. Proposition：首次超限改对了风险标签，仍可能约束错策略

训练分布为 πθ(a|x)=Normal(μθ(x),Σθ(x))，部署分布为 βθ(a|x)=Dirac(clip μθ(x))。两者诱导不同状态分布与风险。即使恰好知道 R_k(πθ)，也不能直接令它等于 R_k(βθ)。

在均值位于裁剪区间内部、协方差非退化的状态，连续高斯对该单点的概率为零，而 Dirac 概率为一。因此 βθ 不绝对连续于 πθ；不能把普通动作似然比替换成“det/gaussian 权重”来精确修正。裁剪会在边界产生原子，但不能提供一般的内部动作覆盖。两个具有正密度的高斯策略之间的原 LR 计算仍合法；缺口发生在据此推断确定性策略时。

此外，首次超限是阈值事件；小动作 Wasserstein 距离本身不保证小事件概率变化。若要从连续成本误差推出事件概率误差，还需要阈值附近概率质量的控制或可信安全余量。

**新增的零交互诊断。** 使用 T1593 的 12 个既有冻结锚点、每点 4 个候选、已记录的 64 回合配对 sample/det 评估与 LR。对首次超限 RTG 使用相同 timeLOO，计算

\[
\widehat{\Delta R}_{\rm LR}
=\frac1n\sum_{i,t}(w_{i,t}-1)\widehat A^{{\rm hit}}_{i,t}.
\]

比较的是候选相对于各自旧策略的风险变化：ΔR_sample、ΔR_det；不会拿 sample 的旧风险直接作为 det 的旧风险。

|48 个候选的任意通道风险变化|平均绝对误差|
|---|---:|
|LR 代理相对于 sample 配对评估|0.040671|
|LR 代理相对于 det 配对评估|0.379501|
|sample 与 det 配对风险变化之差|0.382813|

全部 168 个逐通道及 union 代理概率在 [0,1] 内；没有裁剪概率。2/48 个候选出现代理预测风险下降、det 实测风险上升。以上为后验冻结候选上的描述统计，含有限评估噪声和相关候选，不是新训练试验或显著性结论。它支持优先检验执行核对齐；不证明其解释全部性能损失。

### 3. Conditional proposition：置信上界能限制错误声明，不能产生安全动作

对预先固定的策略和 n 个独立回合，单侧精确 CP 上界满足覆盖率至少 1−α。零事件时，由 (1−p)^n=α 得

\[
U_\alpha(0,n)=1-\alpha^{1/n},\qquad
n_{\min}=\left\lceil\frac{\log\alpha}{\log(1-\delta_{\rm risk})}\right\rceil.
\]

这与 [SciPy 的 exact CP 实现](https://docs.scipy.org/doc/scipy/reference/generated/scipy.stats._result_classes.BinomTestResult.proportion_ci.html)及 [NIST 精确二项上界](https://itl.nist.gov/div898/software/dataplot/refman2/auxillar/exacbino.htm)交叉核验。

4 回合零事件的 95% 单侧上界是 52.713%；312 回合零事件为 0.9556%。后者仍不是零风险，也不能忽略策略选择或多重比较后泛化为全训练过程保证。

|仅作样本量诊断的规格|每次零事件所需回合|25 次验证的环境步数，H=12|
|---|---:|---:|
|单次 95% 置信、风险≤1%|299|89,700；未校正重复检查|
|25 次合计 95% 置信、union 风险≤1%|619|185,700|
|25次×3通道合计95%置信、每通道风险≤1%|728|218,400；同回合评估三个通道|

Bonferroni 不要求各次独立，但每次固定策略的区间必须条件有效。反复查看普通固定样本量 CP 并任选停止时机，不在该保证内。若使用 confidence sequence，需要其条件覆盖假设，不能借名称免去样本需求。

**协议修正。** 不能同时声称保留 25 个更新周期、12k 总环境步、原梯度数据量，又逐轮增加上述验证。允许 5% 风险不是修复，而是改变任务规格；仅列入 sample_complexity.csv 作诊断。δrisk=0 时，单靠有限回合零事件无法给出分布无关的严格零风险证书。

四回合数据无法认证 1% 风险时，拒绝它是“无证书”，不是成功识别不安全；同样也可能拒绝真正安全的策略。旧策略若未认证，no-update 不能被称为已认证 fallback。

### 4. Approximation：修订后的问题必须明确作用于执行策略

任务层概念定义为

\[
\max_\beta J_{\rm raw}(\beta)-\sum_{s\in\mathcal S}\kappa_s\xi_s,
\quad R_h(\beta)\le\delta_h\;(h\in\mathcal H),
\quad J_{c_s}(\beta)\le B_s+\xi_s,\;\xi_s\ge0.
\]

集合由任务语义给出，不是为了实验有结果而任选的标签；K2/K3 本轮不擅自增加允许松弛的通道。严格零灾难还需真实安全支持及递归可行性等额外条件。

内层可使用条件动作运输、旧状态分布代理和信赖域，但若 q 是随机目标策略，部署使用 E(q)，则理论链还需明确

\[
R_h(E(q))\le \widehat R_h(q)
+e_{\rm estimate}+e_{\rm shift}+e_{\rm fit}+e_{\rm execution}.
\]

这是需有效上界才能使用的条件结构，不是已知可计算的小常数。若直接对固定部署策略做独立评估，CP 可认证该策略的风险率目标；无需再把同一评估误差重复加进上述代理误差链，但它不能证明训练过程的每个策略都安全。

两条可行研究路线：直接估计与改进部署策略 β 的风险；或使训练优化的策略与部署策略一致。后一条改变当前评估/执行语义，需要另立协议，不能拿切换指标掩盖失败。本轮优先前一条。

## Remarks and Interpretation

- 第一项优先实验应是冻结候选的部署风险校准与排序验证。当前首次超限代理能解释 sample 风险变化到什么程度、迁移到 det 丢失什么，已有零交互基线。
- 校准模型只能用指定开发数据拟合，新的独立评估验证；旧最终 312 回合不能转为算法选择集后继续称最终测试。
- 原连续累计成本可作为学习信号，首次超限作为最终风险标尺。若构建连续风险上界，需证明上界关系并检验保守性，不能只称其“安全 critic”。
- 修改风险目标、执行核、验证预算和奖励目标应分开比较。风险方案不因支持 OT 接口就构成 OT 的性能贡献。

## Boundaries and Non-Claims

本说明没有训练、没有新增环境交互、没有更改任务预算，没有确认硬安全、原生任务/VLA优势或 SOTA。公式正确与风险预测准确，均不能替代完成训练的奖励—安全结果。T1592/T1593 的 HOLD 结论保持。

## Open Risks

1. 如何以可承受的数据预算得到对 det 部署有效的风险改进信号与校准余量？
2. 连续代理是否覆盖真实阈值风险，是否因过保守降低任务完成率？
3. 多次自适应选策略的统计保证如何分配，如何防止复用测试造成泄漏？
4. 严格零事件规格是否有可验证的安全支持；没有时必须报告缺失条件。
5. 本轮属于 synthetic 机制定位；迁移到异构物理任务/VLA还需正确通道、状态充分性和动作块风险定义。

## Verification Record

`audit_existing.py` 从旧日志和既有 NPZ 独立复算，保存输入哈希；`verify.py` 核对原文件未变、first-hit 恒等式、样本量边界、风险误差分解和汇总。结果见 summary.json、learning_signal.csv、mechanism_risk_proxy.csv、sample_complexity.csv。分析脚本不导入训练器或环境。
