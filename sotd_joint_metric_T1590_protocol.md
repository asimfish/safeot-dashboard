# T-1590 frozen prospective protocol
Question: under unchanged budget/target/shaping5/deterministic deployment and B_excess screening, does conditional Gaussian W2² inside the joint reward/cost trust-region solver add value relative to KL? Not post-hoc score penalties (beta0 throughout), not a full SafeOT solver or novelty claim. T1589 HOLD unchanged.
Only joint solver distance varies: theta(z)=old+D z; maximize finite undiscounted MC centered reward likelihood-ratio surrogate; raw episode cost surrogate <= original B, and distance<=.01*s, s=.5/1/2. KL uses exact old||new original formula and evaluation path. W2²=mean_states sum((mu1-mu0)^2+(exp(ls1)-exp(ls0))^2). D always the original KL-Fisher preconditioned reward/cost gradient columns, CG50, damping.001, same normalization/parameter order. Both metric solves compute and assert identical D per batch. Same SLSQP, ftol1e-7,maxiter60,z0,slack recovery. W2 only replaces last distance coordinate/Jacobian; KKT uses corresponding Jacobian, recovery prices not claimed hard-optimal.
First exploration batch: additional anchor solve KL radius.01 on old/batch, no installation/environment interaction. cW=rawW2²/rawKL of float32 anchor, fixed thereafter. Require accepted nonzero anchor, both distances>1e-12, otherwise stop cell as calibration_degenerate; no alternate radius or eval tuning. Single-direction unit calibration does not make feasible sets identical. W2 solver distance=rawW2²/cW; KL has no extra rescale. Log raw/normalized distance/radius/residual/activation and actual float32 actor checks. Invalid/nonfinite/overradius (>radius+1e-5) candidate excluded; failed solve never labeled valid; recovery slack and hard violation separately recorded.
Per cycle generate original full PPO shadow, then firstcycle anchor if needed, then KL and W2 shadows in fixed order on same old/batch; isolate/restore torch/NumPy RNG. In every arm compute both metrics at that arm's same scale, save actors/D/coordinates/metrics. Main critics/criticAdam from PPO shadow only; native dual once per exploration batch unchanged. Candidate pool old, selected-metric joint, fullPPO; old/T1589 excess selection and paired acceptance identical, 4 episodes each. Joint candidate installs full actor and resets Adam moments; PPO takes PPO moments; rejection/noop restores old actor/Adam. Same main critic/dual update even on rejection.
Everycycle240exploration+144selection+96acceptance=480 actual interactions,including old/noop;25cycles12000,split6000/3600/2400. Final312 raw officialdet episodes additional and unused for training. Selection seed3100000000+seed*100000+cycle*100+k;accept+20+k;eval(seed+1000)*10000+i,environment RNG independent of actor. Both tasks original K2 B[6,6],K3 B[7,6.5,12.5], unchanged targets/H12/shaping5/no shield.
Before training: formulas/autograd finite differences; isotropic fixed-variance proportionality/solution equivalence; anisotropic fixture; six T1589 first-batch offline replay,all36 metric-scale states retained. KL_s1 actor/solver reproduces original. No environment interactions offline. Smoke2tasks×2metrics×3scales seed178,960steps/12eval=12. Smoke pair/replay gate then development2tasks×3seeds180/186/187×2metrics×3scales=36,12000steps/312eval. No early-out because another metric looks better. Invalid cells fail with receipts; fix implementation only, keep old attempts. KL_s1 complete six-run replay includes batch/actor/moments/choices/trajectories/final312 eval exact (float tolerance1e-6 only where explicitly mathematical, no arbitrary stochastic excuse).
Three CPU single-thread workers contingent live CPU/RAM check; no GPU allocation. Controller and final audit/synthesis watcher detached,perjobtimeout2400,commands/PIDs/logs/exits/hash receipts. Historical PPOLag/Fisher/T1589 context reused only matching snapshot/eval protocol; no retraining those.
Prespecified exploratory gate: one COMMON W2 scale with all three seeds joint1 and all channel events0 in BOTH tasks; mean raw reward >= best observed-safe KL scale in each task +.05, and >= safe endpoint K2 PPOLag / K3 Fisher reward. No observed-safe KL point means no comparable safe KL point, NOT a win. Separate KL_s1/Fisher/PPOLag contrasts. Failure→HOLD_NO_EXPANSION; success→PROMISING_NEEDS_NEW_SEEDS, no automatic confirmation here. Three training seeds are units;312episodes not additional trainedreplicas. Allthree scale curves rawreward vs actual any-channel event probability (NOT1-joint), seed dots and seedSD optional, CSV+PNG/SVG, success table. Paired W2−KL perenv/scale,seedrange/worstseed/channelmean,event,excess/trainingrisk/effectiveupdates,compute overhead and active distance/candidate differences.
Publish immutable evidence commit plus incremental homepage progress feed commit after task verification/review. Preserve old owner/entries/otherline data, noforcepush. HTTP cachebust verify first14 and report hash. Final receipt retains fail/success and exact accounting; old results/global trainers/DERIVATION/PDF read-only.
