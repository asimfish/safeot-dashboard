# T1603 independent deployment admission — frozen before observations

Question: does an independent det model scenario eligibility screen reduce scenario-generalization events without merely stalling learning or losing reward? Privileged exact-model diagnostic; no model-free/SafeOT/SOTA claim. All seeds180/186/187 are reused development training seeds. Episodes are conditional estimates, not independent training replicates.

A: T1598 N100 margin0 and T1600 N100 margin.02 final actors, K2/K3 x3 training seeds x2 versions. Each1024 new real det episodes, paired per env/seed. No parameter/optimizer updates. Source actor file and canonical tensor hashes frozen. Raw reward/success/joint, channel cost/event/positive normalized excess, any-event and paired differences.

B: copy T1600 code/snapshot. N100 optimization tapes exact, Bopt=.98 Btask only for middle solver/model installation; all real budgets/observations/PPO/shaping5/native dual/critic/raw inner/raw outer unchanged. K+1 Fisher space,KL.01,200query optimization cap unchanged. Per cycle generate the same three candidates first. Then no-grad det model rollouts for old/pathwise/fullPPO on the same independent256-scene bank. Use sequential float32 cumulative cost at H12 (not parallel sum) and strict > original task budget, no new tolerance. Monitor records only. Gate excludes eventful NEW candidates from original eligibility, including PPO; always keeps old. Original solver-rejected middle stays excluded. All three receive all4 real selection episodes regardless of mask, then original paired4+4 acceptance. 240explore/144selection/96accept=480/cycle;25cycle=12000. No new reward check, no retry/refit/gradient from admission bank. RNG saved/restored and local PCG64 only. Record old bank events and unsafe fallback if final actor is retained old; unsafe old is not certified. All candidate cost matrices, seeds and tape hashes retained. Two arms identical compute order.

C gate: first singleworker import/smoke; K2/K3 monitor seed180 two-cycle exact tensor/optimizer/RNG/batch/proposal/model tape/pathwise/basis/postselection/after/real trace replay vs T1600. Gate two one-cycle480 smoke: original constraints200/300, all3 bank checks,mask/fallback microcases,960? No: each smoke480, replay each960. Check actual installation/rollback and disk reload. Model admission cost per cycle3*256*12=9216; separate from optimization. Numeric adapter checker remains upstream; added deterministic real/model spot check uses existing final trace without new real environment calls. Gates are correctness only, no efficacy stopping.

D matrix: envK2/K3 x seed180/186/187 x monitor/gate =12 jobs from scratch. Fixed12k real steps each,312 existing official det development eval +1024 fresh final det eval. Final only, no checkpoint choice. Monitor full25cycle replay against T1600 additionally audited after runs. Max4 singlethreadCPU processes,2h perjob (including final eval),6h entire batch from controller launch; controller stops dispatch at deadline, kills own over-deadline processes with receipt, no automatic retries/duplicates. Gate failure stops development but A can still be accounted independently; all failures saved. No GPU.

Frozen seed namespace (PCG64 accepts uint64): seed_index={180:0,186:1,187:2};env_index={K2:0,K3:1}.
A episode ID=1603000000000+env_index*10000000+seed_index*100000+i, i0..1023.
E episode ID=1603200000000+env_index*10000000+seed_index*100000+i.
Admission development ID=1603100000000+env_index*10000000+seed_index*100000+cycle*256+i (i0..255,cycle0..24).
Replay admission IDs use1603400000000; smoke use1603300000000. Pair IDs across version/arm intentionally; distinct task/seed/cycle/split never overlap. Each candidate is frozen before exposing current bank. Numpy local default_rng(ID): initial normal(0,.02,2), then12normal noise draws; hash actual initial/noise arrays. Det uses zero action noise, no exploration RNG. Existing stochastic training stream unchanged; ID isolation checked against actual stored old model/select/accept/final and diagnostic seed lists plus historical numeric seed artifacts. E evaluated only after corresponding training completes; A cannot alter B/E.

Costs planned: training144000 + oldfinal44928 +freshE147456 +A147456 =483840 real steps; replay1920/smoke960 separately. Admission development2764800 model steps, gates55296 modelsteps; optimization model counts separate (up to72M for12jobs). Old model query budget excludes extra admission, explicitly logged. Audit no extra learning. Paired banks A/E remain separate; no failing trace returned to training.

Decision frozen: HOLD unless all six gate final fresh policies show zero observed any-event, joint >= paired monitor each seed, and task mean raw reward delta>=-.1. Then PROMISING_REQUIRES_FRESH_TRAINING_SEEDS only. Also show old312 table and monitor risks, seedwise differences and worst seed, rejection/source rates,old retention,unsafe_fallback, longest no-update streak. Fewer events with no effective learning or severe reward loss is not repair.0/1024 is not zero population risk. No confidence claims from3 training seeds. No automatic margin/seed/native/VLA expansion.
